Program is meant to be run from the command line.

A language and input file name must be specified at runtime, it is setup to run using english currently.

To run program: java PipeAndFilter english kjbible.txt